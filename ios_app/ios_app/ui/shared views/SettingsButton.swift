import Foundation
import SwiftUI

struct SettingsImage: View {
    var body: some View {
        Image(systemName: "gearshape")
            .foregroundColor(.black)
            .padding(.trailing, 20)
    }
}
